name: Docker Registry Test 
on: [push] # When pushing to any branch then run this action
# Env variable
env:
  DOCKER_USERNAME: ${{secrets.DOCKER_USERNAME}}
  DOCKER_PASSWORD: ${{secrets.DOCKER_PASSWORD}}
  REGISTRY: ${{secrets.REGISTRY}}
jobs:
  push-image-to-docker-hub:  # job name
    runs-on: ubuntu-latest  # runner name : (ubuntu latest version) 
    steps:
    - uses: actions/checkout@v2 # first action : checkout source code
    - name: docker login
      run: | # log into docker hub account
        #cat /etc/docker/daemon.json
        #echo '{exec-opts: [native.cgroupdriver=cgroupfs], cgroup-parent: /actions_job , insecure-registries: ["35.202.29.36"]}' | sudo tee -a /etc/docker/daemon.json
        #sudo su & rm /etc/docker/daemon.json
        #sudo echo '{exec-opts: [native.cgroupdriver=cgroupfs], cgroup-parent: /actions_job , "insecure-registries": ["35.202.29.36"]}' >> /etc/docker/daemon.json 
        sudo rm /var/run/docker.pid
        sudo dockerd --insecure-registry "35.202.29.36"
        #sudo systemctl daemon-reload
        #sudo systemctl start docker
        docker info
        sudo docker login http://$REGISTRY -u $DOCKER_USERNAME -p $DOCKER_PASSWORD  
    - name: Get current date # get the date of the build
      id: date
      run: echo "::set-output name=date::$(date +'%Y-%m-%d--%M-%S')"
    # - name: Build the Docker image # push The image to the docker hub
    #   run: docker build . --file Dockerfile --tag $DOCKER_USER/$REPO_NAME:${{ steps.date.outputs.date }}
    # - name: Docker Push
    #   run: docker push $DOCKER_USER/$REPO_NAME:${{ steps.date.outputs.date }}
